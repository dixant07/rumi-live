# Dependencies (exclude node_modules but NOT from games - they need to be built)
node_modules
.pnpm-store

# Build outputs from root (NOT from games folder - we need source to build)
.next
out
build

# Development
.git
.gitignore
.vscode
.idea
*.log

# Environment files (secrets should be set in DigitalOcean dashboard)
.env
.env.*
!.env.template

# Docker (don't copy Dockerfile into context, it's already being used)
docker-compose*
.docker

# Documentation
*.md
!README.md
docs

# Testing
coverage
.nyc_output
*.test.*
*.spec.*

# Misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Games - exclude node_modules and dist from games (they'll be rebuilt)
games/**/node_modules
games/**/dist
games/**/.git

# Matchmaking (if exists)
matchmaking/node_modules
matchmaking/dist

# Public games dist (will be replaced by built games)
public/games
